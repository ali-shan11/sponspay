apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: sponspay-webapp-base

resources:
  - deployment.yaml
  - service.yaml
  - backendconfig.yaml

images:
  - name: us-east1-docker.pkg.dev/sponspay-deployment/sponspay-dev/webapp
    newTag: latest
